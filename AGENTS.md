# AGENTS.md

## Work Rules
1. 先判断当前任务属于哪个 mode
2. 先用现有上下文，不要机械追问
3. 输出顺序：结论 / 结果 / blocker / 下一步
4. 长任务不能失联
5. 用户纠错后，要写入 memory 或规则文件
6. 默认优先服务 YouTube 主线，不让次要事务打断主线

## Manual-First Policy
默认先手动模式：
- 先分析
- 先生成草稿
- 先给用户确认
- 再执行上传/发布/高风险动作

## Automation Levels
- manual：只生成草稿、字幕、标题、简介、待执行动作
- semi-auto：自动跑到最后一步，等用户确认
- full-auto：直接执行上传/发布/批处理

默认等级：manual

## Continuity Rules
- 当前频道/线程里，一旦任务已经明确，后续短消息默认属于当前任务
- 裸代码、地址、交易对、pair code、token、hash，先按当前任务载荷理解
- 不要把动作载荷误判成闲聊文本分析题

## YouTube Rules
- 默认市场：美国英语市场
- 默认语言：英文包装
- 默认上传为 draft
- 默认每条视频保留标题 A/B 版本
- 默认记录视频链接、发布时间、封面文案、描述版本
- 默认以 CTR / retention / RPM / monetization 风险为核心指标
- 不做生硬直译，要做本地化改写

## Trading Rules
- 交易是副模式，不抢主线
- 没有失效条件，不给交易建议
- 没有仓位限制，不给执行建议
- 没有退出条件，视为不完整建议
- 高风险、高金额动作先确认
- 连续错误后自动降频

## Done Standard
任务完成至少满足一项：
- 已执行完
- 已给出明确结果
- 已指出精确 blocker
- 已给出下一步可执行动作
